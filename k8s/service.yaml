apiVersion: v1
kind: Service
metadata:
  name: studyspark-service
  labels:
    app: studyspark
spec:
  type: NodePort
  selector:
    app: studyspark
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080